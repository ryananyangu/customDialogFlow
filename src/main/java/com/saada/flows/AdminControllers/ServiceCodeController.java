package com.saada.flows.AdminControllers;

public class ServiceCodeController {
    // TODO: User view all sortCodes (Admin) AND User approve shortcode status (Admin) AND User update shortCode(Admin) AND User delete shortcode(Admin)
}